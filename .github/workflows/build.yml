name: Test Build

on:
  push:
  pull_request:

jobs:
  build:
    uses: BKPepe/actions-shared-workflows/.github/workflows/multi-arch-test-build.yml@improveMatrixMultiArch
    with:
      matrix: |
        [
          {"arch": "x86_64", "target": "x86-64", "runtime_test": true}
        ]
    secrets: inherit
